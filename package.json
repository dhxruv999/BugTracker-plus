{
  "name": "bugtracker-plus",
  "private": true,
  "version": "1.0.0",
  "description": "BugTracker+ monorepo",
  "scripts": {
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev"
  },
  "dependencies": {
    "bcrypt": "^6.0.0"
  }
}
